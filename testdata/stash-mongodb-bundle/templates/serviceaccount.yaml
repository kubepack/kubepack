{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "stash-mongodb-bundle.serviceAccountName" . }}
  labels:
{{ include "stash-mongodb-bundle.labels" . | nindent 4 }}
{{- end -}}
