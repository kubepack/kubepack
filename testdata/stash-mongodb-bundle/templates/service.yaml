apiVersion: v1
kind: Service
metadata:
  name: {{ include "stash-mongodb-bundle.fullname" . }}
  labels:
    {{- include "stash-mongodb-bundle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stash-mongodb-bundle.selectorLabels" . | nindent 4 }}
