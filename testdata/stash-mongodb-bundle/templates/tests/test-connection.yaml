apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stash-mongodb-bundle.fullname" . }}-test-connection"
  labels:
{{ include "stash-mongodb-bundle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "stash-mongodb-bundle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
