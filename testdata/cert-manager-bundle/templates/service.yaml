apiVersion: v1
kind: Service
metadata:
  name: {{ include "cert-manager-bundle.fullname" . }}
  labels:
    {{- include "cert-manager-bundle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cert-manager-bundle.selectorLabels" . | nindent 4 }}
