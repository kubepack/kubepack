{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubedb-catalog-bundle.serviceAccountName" . }}
  labels:
{{ include "kubedb-catalog-bundle.labels" . | nindent 4 }}
{{- end -}}
