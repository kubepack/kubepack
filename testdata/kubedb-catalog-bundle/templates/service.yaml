apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubedb-catalog-bundle.fullname" . }}
  labels:
    {{- include "kubedb-catalog-bundle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubedb-catalog-bundle.selectorLabels" . | nindent 4 }}
