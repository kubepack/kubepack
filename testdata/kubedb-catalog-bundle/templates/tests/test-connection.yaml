apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubedb-catalog-bundle.fullname" . }}-test-connection"
  labels:
{{ include "kubedb-catalog-bundle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kubedb-catalog-bundle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
