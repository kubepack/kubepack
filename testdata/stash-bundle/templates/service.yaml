apiVersion: v1
kind: Service
metadata:
  name: {{ include "stash-bundle.fullname" . }}
  labels:
    {{- include "stash-bundle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stash-bundle.selectorLabels" . | nindent 4 }}
