apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stash-bundle.fullname" . }}-test-connection"
  labels:
{{ include "stash-bundle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "stash-bundle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
