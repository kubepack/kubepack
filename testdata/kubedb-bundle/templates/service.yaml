apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubedb-bundle.fullname" . }}
  labels:
    {{- include "kubedb-bundle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubedb-bundle.selectorLabels" . | nindent 4 }}
