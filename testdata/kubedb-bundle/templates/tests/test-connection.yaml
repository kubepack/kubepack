apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubedb-bundle.fullname" . }}-test-connection"
  labels:
{{ include "kubedb-bundle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kubedb-bundle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
